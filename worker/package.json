{
  "name": "netpulse-checks",
  "version": "1.0.0",
  "private": true,
  "description": "NetPulse check worker fuer HTTP, HTTPS und Ping Healthchecks",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
